# ============================================
# SupaSquad Environment Variables
# ============================================
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to version control
# ============================================

# --------------------------------------------
# Supabase Configuration (REQUIRED)
# --------------------------------------------
# Get these from your Supabase project dashboard:
# https://app.supabase.com/project/_/settings/api

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# --------------------------------------------
# Security Keys (REQUIRED for production)
# --------------------------------------------
# Generate TOKEN_ENCRYPTION_KEY with:
#   openssl rand -base64 32
#
# Generate OAUTH_STATE_SECRET with:
#   openssl rand -hex 32
#
# CRITICAL: These must be set in production or OAuth will fail

TOKEN_ENCRYPTION_KEY=your-32-byte-base64-encoded-key
OAUTH_STATE_SECRET=your-random-secret-string

# --------------------------------------------
# App Configuration
# --------------------------------------------
# Set this to your production URL (required for OAuth callbacks)
# Do NOT include trailing slash

NEXT_PUBLIC_APP_URL=https://your-domain.com

# --------------------------------------------
# OAuth Provider Credentials
# --------------------------------------------
# You must create OAuth apps with each provider and add your
# credentials here. See docs/OAUTH-SETUP.md for detailed instructions.
#
# Callback URLs to register with each provider:
#   Discord:  https://your-domain.com/api/auth/callback/discord
#   LinkedIn: https://your-domain.com/api/auth/callback/linkedin
#   GitHub:   https://your-domain.com/api/auth/callback/github
#   Twitter:  https://your-domain.com/api/auth/callback/twitter

# Discord (https://discord.com/developers/applications)
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=

# LinkedIn (https://www.linkedin.com/developers/apps)
LINKEDIN_CLIENT_ID=
LINKEDIN_CLIENT_SECRET=

# GitHub (https://github.com/settings/developers)
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Twitter/X (https://developer.twitter.com/en/portal/dashboard)
TWITTER_CLIENT_ID=
TWITTER_CLIENT_SECRET=

# --------------------------------------------
# Activity Sync Configuration
# --------------------------------------------
# Required for the /api/sync/activities endpoint
# Generate with: openssl rand -hex 32

SYNC_API_KEY=your-sync-api-key

# Supabase service role key (for server-side database access)
# Get from: https://app.supabase.com/project/_/settings/api
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --------------------------------------------
# Optional: Development/Debug Settings
# --------------------------------------------
# Uncomment to enable debug logging for OAuth flows
# OAUTH_DEBUG=true
